# =============================================================================
# ФАЙЛ: rental/apps.py
# =============================================================================
# НАЗНАЧЕНИЕ:
#   Конфигурация Django-приложения rental.
#   Определяет метаданные приложения и инициализацию при запуске.
#
# ФУНКЦИИ:
#   - Указывает имя приложения для Django
#   - Задаёт человекочитаемое название
#   - Подключает сигналы при загрузке приложения
#
# ПРОЕКТ: ООО "ИНТЕРЬЕР" - Сайт аренды помещений
# =============================================================================

"""
КОНФИГУРАЦИЯ ПРИЛОЖЕНИЯ
"""

from django.apps import AppConfig


class RentalConfig(AppConfig):
    """
    Класс конфигурации приложения rental.

    Attributes:
        default_auto_field: Тип автоинкремента для первичных ключей
        name: Техническое имя приложения (должно совпадать с директорией)
        verbose_name: Человекочитаемое название для админки
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'rental'
    verbose_name = 'Аренда помещений'

    def ready(self):
        """
        Метод вызывается Django при полной загрузке приложения.
        Здесь подключаем сигналы для:
        - Автоматического создания профиля при регистрации
        - Пересчёта рейтинга при добавлении/удалении отзыва
        """
        import rental.signals  # noqa: F401 - импорт нужен для регистрации сигналов
